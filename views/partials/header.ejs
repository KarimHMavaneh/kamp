<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Campground Demo</title>

    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.css"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/stylesheets/main.css" />
    <!-- <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.4.2/dist/semantic.min.css"> -->
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="/">K-Camp</a>

      <button
        class="navbar-toggler"
        type="button"
        data-toggle="collapse"
        data-target="#navbarNavAltMarkup"
        aria-controls="navbarNavAltMarkup"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div
        class="collapse navbar-collapse justify-content-end mr-auto"
        id="navbarNavAltMarkup"
      >
        <div class="navbar-nav text-right">
          <% if(!currentUser){ %>
          <li><a class="nav-item nav-link" href="/login">Login</a></li>
          <li><a class="nav-item nav-link" href="/register">Sign Up</a></li>
          <% }else{ %>

          <li>
            <a class="nav-item nav-link" href="#"
              >LogedIn User: <%= currentUser.username %>
            </a>
          </li>
          <li><a class="nav-item nav-link" href="/logout">Logout</a></li>

          <% } %>
        </div>
      </div>
    </nav>
    <div class="container">
      <% if(danger && danger.length>0){ %>
      <div class="alert alert-danger" role="alert">
        <%= danger %>
      </div>
      <% } %> <% if( success && success.length > 0 ){ %>
      <div class="alert alert-success" role="alert">
        <%= success %>
      </div>
      <% } %>
    </div>
  </body>
</html>
